import React from 'react'

const Expense = () => {
    return(
        <div>
            <h2>Expense </h2>
            <p>Should show current expense here</p>
            <p>Income should be red</p>
            <p>add an input spot where it will add inputs to a transaction History section</p>
            <p>Make sure to include a delete button </p>
            {/* <p> As more income is added the income value should go up</p>
            Ill need a form to gather user input, ill store that input into a state within my page component, pass that state to this component, same with the dollar value state , create a function keeps adding the state to itself */}
        </div>
    )
}


export default Expense